// Per-rail adapters live in subdirectories of this folder.
// Each adapter implements the `Adapter` interface from `../core/contract.js`.
// The Telegram adapter (Phase 1) is the reference implementation.
export {};
